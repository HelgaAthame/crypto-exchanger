# CryptoSwap — план реализации (для AI-агента)

> Этот файл — техническое ТЗ и пошаговый план для Claude Code / другого AI-агента,
> который будет реализовывать проект в отдельной сессии. Пиши код по шагам ниже,
> отмечая ✅/🚧/⬜ статусы прямо в этом файле по мере продвижения (как живая
> документация — аналогично плану соседнего проекта PulseOps).

---

## 0. Что это

**CryptoSwap** — обменник валют: фиат → крипта, крипта → фиат, крипта → крипта.
Пользователь выбирает валюту "отдаю" и валюту "получаю", вводит сумму, видит курс
и итоговую сумму к получению (с учётом комиссии), может создать заявку на обмен.

**Важно: это НЕ реальный обменник с реальными деньгами.** Это демонстрационный
портфолио-проект — заявки создаются, курсы реальные (публичное API), но фактического
перевода средств не происходит (нет интеграции с реальными платёжными провайдерами/
крипто-кошельками с приватными ключами). Это должно быть явно и честно написано в
README и, если есть смысл, в UI (баннер, как в PulseOps: "Demo — no real funds move").

**Портфолио-цель:** отдельный проект от PulseOps (другой домен: не SaaS-аналитика,
а финтех/обменник), чтобы показать широту. Одна четкая история, не смешивать с
другими проектами.

---

## 1. Легенда / позиционирование (придумать так же честно, как в PulseOps)

Перед началом реализации определить и вписать в README:
- **Что это:** свой pet/MVP-проект обменника (аналог small crypto exchanger типа
  простых сайтов-агрегаторов курсов).
- **Для кого:** обычные пользователи, которым нужно быстро прикинуть курс обмена
  фиат↔крипта или крипта↔крипта без захода на биржу.
- **Проблема:** курсы разбросаны по разным сайтам/биржам, нет единого удобного
  калькулятора с историей заявок.
- **Решение:** один экран — выбрал валюты, увидел живой курс и сумму, создал заявку
  (демо, без реального перевода денег).
- **Твоя роль:** соло — продукт, дизайн, фуллстек.

НЕ выдумывать реального работающего биттех-бизнеса с реальными объёмами/клиентами —
это создаст регуляторные вопросы на собеседовании (обмен крипты требует лицензий
в реальном мире). Чётко: "pet-проект/MVP, демонстрирует техническую реализацию
конвертера, без реальных переводов денег".

---

## 2. Функциональные требования

### 2.1 Базовый функционал (MVP)
- [x] Выбор направления обмена: селекторы валют покрывают Fiat→Crypto,
      Crypto→Fiat, Crypto→Crypto (единый список валют, любая пара)
- [x] Селекторы валюты "отдаю" / "получаю" (тикер + название)
- [x] Поле ввода суммы в обе стороны: ввод с любой стороны пересчитывает
      другую (`computeExchangeAmount` с direction "give"/"receive") ✅
- [x] Живой курс валют (см. раздел 4 — источник данных) — `/api/rates`
- [x] Комиссия обменника (фикс 1.5%, `DEFAULT_FEE_PERCENT`) — явно показана
      пользователю в разбивке (курс, комиссия, итог к получению)
- [x] Кнопка "Создать заявку на обмен" → простая форма (контакт, демо без
      реальной валидации платёжных данных)
- [x] Список заявок пользователя (история) со статусами: pending → completed
      (автозавершение через 15 секунд, `/history`)
- [x] Страница отдельной заявки с деталями (`/exchange/[id]`)

### 2.2 Расширенный функционал (после MVP, по желанию)
- [x] График истории курса выбранной пары (7/30/90 дней) ✅
- [x] Список поддерживаемых валют с текущими курсами — `/rates` ✅
- [x] Уведомление/алерт "сообщить, когда курс достигнет X" — демо, проверка в
      браузере, страница `/alerts` (раздел 17) ✅
- [x] Мультиязычность (en/ru) — свой словарь + `translate`, переключатель
      в шапке, коды ошибок валидации вместо готовых фраз ✅
- [x] Тёмная/светлая тема ✅

### 2.3 Явно НЕ делать (осознанно вне скоупа)
- Реальную интеграцию с платёжными системами / крипто-кошельками с приватными
  ключами
- KYC/AML верификацию
- Хранение реальных средств пользователей
- Реальные выводы/переводы денег

---

## 3. Технологический стек (рекомендация, можно адаптировать)

Выбирать стек в начале сессии реализации, здесь — отправная точка:

| Область | Рекомендация | Примечание |
|---|---|---|
| Фреймворк | Next.js (App Router) или Vite+React | Next.js — если нужен SSR/API routes для проксирования курсов (спрятать API-ключи) |
| Язык | TypeScript strict | |
| Стили | Tailwind CSS | |
| UI-кит | shadcn/ui или свой набор | |
| БД | Postgres (Supabase free tier) ИЛИ SQLite для совсем простого MVP | Нужна только для истории заявок — можно начать вообще без БД (localStorage) для первой итерации, добавить БД во второй |
| ORM | Drizzle (если Postgres) | Тот же выбор, что в PulseOps — переиспользовать опыт |
| Auth | Опционально. Можно вообще без авторизации на MVP (заявки анонимные, привязаны к browser-сессии), добавить позже | Не усложнять на старте |
| Валидация | zod | |
| Тесты | Vitest — покрыть доменную логику (расчёт курса/комиссии) на 100%, как в PulseOps | |
| Деплой | Vercel (free tier) | |

**Принцип:** начать МАКСИМАЛЬНО просто (без БД, без auth) — калькулятор курса,
работающий на реальных данных, это уже рабочий MVP. БД и auth добавлять
итерацией 2, когда появится нужда сохранять историю заявок между визитами.

---

## 4. Источник данных о курсах (ключевой технический вопрос)

Нужны два типа курсов:
1. **Фиат↔фиат / фиат↔крипта** курсы
2. **Крипта↔крипта** курсы

### Рекомендуемые бесплатные API (проверить актуальность на момент реализации,
### у всех есть free tier):

- **CoinGecko API** (`api.coingecko.com`) — бесплатно, без API-ключа для базовых
  запросов, даёт курсы крипты к фиату и крипты к крипте (через фиат-мост).
  Простой и надёжный вариант для старта — **рекомендуется как основной источник**.
- **exchangerate-api.com** или **Frankfurter API** (frankfurter.dev, бесплатно,
  без ключа) — для чистых фиат↔фиат курсов, если нужны отдельно от крипты.
- **CoinMarketCap API** — тоже есть free tier, альтернатива CoinGecko.

**Архитектурное решение:** курсы запрашивать на **сервере** (API route/Route
Handler), не с клиента напрямую — чтобы (а) спрятать API-ключ, если он нужен,
(б) закэшировать на несколько секунд/минут и не упираться в rate-limit,
(в) держать единую точку, откуда UI берёт данные (легко подменить провайдера).

Пример потока:
```
Клиент → GET /api/rates?from=BTC&to=USD → сервер идёт в CoinGecko (с кэшем
in-memory/Redis на ~30-60 сек) → возвращает { rate, updatedAt }
```

Курс "крипта → крипта" считать через кросс-курс к общей базовой валюте (USD):
`rate(A→B) = rate(A→USD) / rate(B→USD)`.

---

## 5. Доменная логика (то, что тестируется на 100%, по аналогии с PulseOps)

Вынести в чистые функции без побочных эффектов (легко покрыть Vitest):

```
computeExchangeAmount(input: {
  amount: number;
  direction: "give" | "receive"; // с какой стороны введена сумма
  rate: number;        // курс "отдаю"→"получаю", по данным API
  feePercent: number;  // комиссия обменника, например 1.5
}): {
  giveAmount: number;
  receiveAmount: number;
  feeAmount: number;
  effectiveRate: number; // курс с учётом комиссии
}
```

```
computeCrossRate(rateAtoUSD: number, rateBtoUSD: number): number
// курс A→B через общую базу USD
```

```
validateExchangeRequest(input): { valid: boolean; errors: string[] }
// валидация суммы (мин/макс лимиты), валют (пара поддерживается), и т.д.
```

Эти функции — ядро приложения. Именно они должны быть покрыты тестами на 100%,
аналогично `computeAnalytics`/`computeMrrMovement`/`computeCohortRetention` в
PulseOps. UI и API routes — тонкий слой поверх них.

---

## 6. Структура данных заявки на обмен

```ts
type ExchangeRequest = {
  id: string;
  createdAt: Date;
  status: "pending" | "completed" | "cancelled"; // demo-статусы, без реальной обработки
  giveCurrency: string;    // напр. "BTC"
  receiveCurrency: string; // напр. "USD"
  giveAmount: number;
  receiveAmount: number;
  feeAmount: number;
  rateAtCreation: number;  // зафиксированный курс на момент заявки
  recipientContact: string; // demo: email или произвольная строка, без реальной валидации платёжных реквизитов
};
```

---

## 7. Экраны / маршруты

- `/` — главный экран калькулятора (выбор валют, сумма, курс, кнопка создать заявку)
- `/exchange/[id]` — детали конкретной заявки, статус
- `/history` — список заявок (текущей browser-сессии или пользователя, если будет auth)
- `/rates` (опционально, расширенный функционал) — таблица всех поддерживаемых валют с курсами

---

## 8. UI/UX ориентиры

- Взять вдохновение из настоящих обменников: Wise, CEX.io, простых crypto-виджетов
  (SimpleSwap, ChangeNOW — для референса дизайна, НЕ копировать бренд/логотип)
- Один экран — главное действие (как калькулятор), не перегружать
- Живое обновление курса (или кнопка "обновить курс", если нет вебсокетов)
- Чёткое отображение комиссии — прозрачность = доверие, даже в демо-версии
- Явный демо-баннер (аналогично PulseOps: "Demo project — no real funds are
  transferred. Rates are live, requests are simulated.")
- Тёмная тема — опционально, но по опыту PulseOps смотрится статуснее для портфолио

---

## 9. Порядок реализации (пошагово для AI-агента)

1. [x] **Инициализация проекта** (Next.js 16 + TS strict + Tailwind v4), базовый
   layout, README
2. [x] **Доменная логика курсов и расчёта обмена** (раздел 5) + тесты на 100%
   (`src/lib/exchange-calc.ts`, `src/lib/currencies.ts`, 23 теста, 100% coverage)
3. [x] **API route** для получения курсов (раздел 4) с кэшированием —
   `GET /api/rates`, in-memory кэш 60с, CoinGecko + Frankfurter
4. [x] **Главный экран-калькулятор**: селекторы валют, инпут суммы, живой
   пересчёт, отображение комиссии (`/`)
5. [x] **Создание заявки** (localStorage, без БД) + страница деталей заявки
   (`/exchange/[id]`)
6. [x] **История заявок** (`/history`) из localStorage
7. [x] **Полировка UI**: демо-баннер, тёмная тема (next-themes), базовые
   loading/error состояния в калькуляторе; отдельные `loading.tsx`/`error.tsx`
   файлы для роутов — не сделаны (см. раздел 10)
8. [ ] **(Итерация 2, опционально)** Добавить БД (Supabase/Postgres) для
   персистентной истории между визитами/устройствами, возможно — простую auth
9. [ ] **(Итерация 2, опционально)** График истории курса, таблица всех валют,
   алерты по курсу
10. [x] **README как case study** (Problem → Solution → My role → Architecture →
    Tech stack → Testing)
11. [ ] **Деплой на Vercel** — не выполнялось в этой сессии

---

## 10. Чеклист качества (перенести из практики PulseOps)

- [x] Доменная логика (расчёт курса/комиссии/кросс-курса) покрыта тестами на 100%
      (Vitest), framework-glue (API routes, UI, rates/history-store) сознательно
      вне coverage
- [x] TypeScript strict, `tsc --noEmit` чистый (проверено)
- [x] `npm run build` (prod-билд) зелёный (проверено)
- [x] loading.tsx / error.tsx / not-found.tsx / global-error.tsx — сделаны,
      в фирменном оформлении
- [🚧] Responsive — вёрстка адаптирована (бургер ниже `md`, табы 2×2 ниже `xs`,
      таблицы со скроллом), но **на реальных устройствах не проверялась**:
      MCP Playwright почти всю сессию был отключён
- [x] Честный демо-баннер, нет вводящих в заблуждение формулировок о реальности
      переводов
- [x] README на английском, как product case study, с честной легендой (раздел 1)
- [x] Коммиты — логичными шагами, на английском, запушены в `main`

---

## 11. Открытые вопросы (решить в начале сессии реализации)

- Нужна ли БД сразу, или начинать с localStorage (рекомендация: начать без БД)?
- Нужна ли авторизация вообще, или анонимные заявки на базе browser-сессии
  (рекомендация: без авторизации на MVP)?
- Какой конкретно бесплатный API курсов использовать — проверить, что CoinGecko
  всё ещё бесплатен без ключа на момент реализации (провайдеры меняют политику)
- Список поддерживаемых валют на старте — рекомендация: 3-5 фиатов (USD, EUR, RUB)
  + 5-8 популярных крипто (BTC, ETH, USDT, USDC, SOL, BNB) — не пытаться
  поддержать всё сразу

---

## 12. Бренд и логотип

**Название проекта: «Crypto Exchanger»** (заменяет более раннее рабочее
название RateBridge — переименовать везде: `README.md`, `package.json`,
`<title>`/metadata, NavBar, тексты в UI).

Источник вдохновения — исходные растровые логотипы в `public/logo/`
(`logo.png` — полный, с текстом «CRYPTO EXCHANGER»; `logo-e.png` — только
иконка "E" в кольце из двух стрелок, чёрный фон, золотой 3D-рендер).
Так как это фотореалистичный растровый рендер, а не векторная графика,
исходники не трассируются один в один — вместо этого от руки собирается
SVG-версия того же мотива (кольцо из двух стрелок + буква "E" внутри),
воспроизводящая тот же silhouette и luxury-ощущение через:
- золотой линейный/радиальный градиент (тёмное золото → светлое золото →
  тёмное золото, имитация металлического блика);
- `<filter>` с `feDropShadow`/`feGaussianBlur` для мягкой тени и объёма;
- прозрачный фон (без чёрной подложки), чтобы логотип ложился на светлую
  и тёмную тему сайта.

Результат:
- `public/logo/logo-mark.svg` — только иконка (кольцо + "E"), прозрачный
  фон, обрезка (trim) по границам непустых пикселей — используется в NavBar
  и как источник для фавикона;
- `public/logo/logo-full.svg` — иконка + текст «CRYPTO EXCHANGER» под ней,
  тот же градиент/стиль — для страниц, где нужен полный логотип (например,
  README, OG-превью в будущем);
- `src/app/favicon.ico` — сгенерирован из `logo-mark.svg` (рендер в PNG
  через `sharp`, обрезка пустых боковых/верхних/нижних пикселей, упаковка
  в `.ico` с размерами 16/32/48/64).

**Фирменный стиль (design tokens), применить в `globals.css` /
Tailwind-теме:**
- Основной акцент — золото: `--brand-gold` (например `#D4AF37`) с
  градиентом от `#8a6a1f` до `#f4e2a1` для крупных элементов (логотип,
  акцентные кнопки/иконки).
- Тёмная тема ближе к исходному лого — почти чёрный фон (`#0a0a0a`/
  `#111111`), золото как основной акцентный цвет вместо стандартного
  синего/фиолетового акцента.
- Светлая тема — светлый фон, тот же золотой акцент для кнопок/ссылок/
  фокуса, чтобы бренд считывался в обеих темах.
- NavBar использует `logo-mark.svg` рядом с текстовым названием
  «Crypto Exchanger» (это заменяет текущий чисто текстовый лейбл).

---

## 13. Современный дизайн-стиль (референсы + design system)

Референсы (изучены через выдержки/структуру страниц, где не было прямого
визуального доступа — импровизация на основе общих паттернов этих
продуктов):
- **MoonPay** (`/buy`) — модульные карточки, пошаговый флоу покупки,
  крупные CTA-кнопки, ряд логотипов платёжных систем как визуальное
  подтверждение доверия, карусель отзывов.
- **Changelly** (`/buy/btc`), **CoinGate** (`/buy`) — простой калькулятор
  на первом экране: два поля (отдаю/получаю), явный курс, минимум лишних
  элементов, быстрый переход к действию.
- **Topper**, **Simplex** — светлые, воздушные формы, скруглённые крупные
  поля ввода, дружелюбная типографика, акцент на простоте одного экрана.
- **Crypto.com** — крупные заголовки-хиро, высокий контраст, карточная
  структура, акцентные градиенты на кнопках, ощущение масштаба/доверия
  через цифры (кол-во пользователей и т.п. — в нашем демо-проекте не
  применимо буквально, но сам приём "крупная цифра = доверие" переносим
  на крупный курс обмена).

**Вывод: калькулятор должен быть один экран-виджет** (как у Changelly/
CoinGate), но визуально оформлен на уровне MoonPay/Crypto.com — с крупной
карточкой, мягкими тенями, градиентами и внятной иерархией, а не просто
формой с бордерами.

**Design system (обновляет раздел 12, применяется в `globals.css` +
компонентах):**
- **Форма:** крупные скругления (`rounded-2xl`/`rounded-3xl`) вместо
  острых углов, мягкие многослойные тени (`shadow-lg`/`shadow-black/20`)
  вместо тонких бордеров как основного разделителя.
- **Фон:** в тёмной теме — не плоский чёрный, а лёгкий радиальный
  градиент/vignette (тёмно-серый → почти чёрный) за карточкой калькулятора,
  чтобы карточка "парила" (как hero-секции MoonPay/Crypto.com). В светлой
  теме — мягкий светло-серый/кремовый фон с едва заметным золотым glow.
- **Акценты и CTA:** главная кнопка — золотой градиент
  (`linear-gradient(135deg, gold-light, gold-mid, gold-dark)`) с лёгким
  подъёмом/тенью при hover, а не плоская заливка `--accent`.
- **Курс обмена — главный визуальный элемент:** крупная типографика для
  суммы "к получению" (по аналогии с тем, как Crypto.com/MoonPay делают
  акцент на крупных цифрах), а не мелкий текст в списке.
- **Карточка калькулятора:** двойной блок "Отдаю / Получаю" с крупными
  полями ввода, разделённый кнопкой swap (⇅) в виде круглой кнопки со
  сдвигом на границу блоков (визуальный приём из Changelly/CoinGate).
- **NavBar:** `sticky` + полупрозрачный фон с `backdrop-blur` (стеклянный
  эффект, как в современных фин-тех лендингах), лого + название слева,
  переключатель темы справа.
- **Demo-баннер:** не блеклая плашка, а золотая "pill"-плашка с иконкой —
  привлекает внимание, но не выглядит как предупреждение об ошибке.
- **Типографика:** сохраняется текущий Geist Sans, но с более выраженной
  иерархией размеров (крупный `text-3xl`/`text-4xl` заголовок, чёткий
  `text-sm text-muted` для второстепенного текста).
- **Респонсивность:** карточка калькулятора остаётся `max-w-md`, но с
  большими отступами и "воздухом" на десктопе (либеральный `py`/`gap`),
  как на референсных лендингах, а не плотная форма.

Компоненты, которые обновляются под этот стиль: `globals.css` (токены,
фон), `nav-bar.tsx`, `demo-banner.tsx`, `exchange-calculator.tsx`,
`currency-select.tsx`.

### 13.1 Бегущая строка курсов (rates ticker)

Приём, знакомый по биржам и обменникам (и явно нравящийся заказчику):
горизонтальная «бегущая строка» с курсами, идущая под шапкой сайта.
Делает страницу живой и сразу показывает, что курсы настоящие.

Требования:
- **Реальные данные, не заглушка** — строка питается тем же серверным
  слоем курсов, что и калькулятор (CoinGecko + Frankfurter через
  in-memory кэш), новый Route Handler `GET /api/ticker`, отдающий список
  `{ code, name, usdPrice, change24h }` по всем поддерживаемым валютам.
  Для крипты 24-часовое изменение берётся у CoinGecko
  (`include_24hr_change=true`), для фиата — не показывается либо считается
  как изменение к USD (если провайдер даёт исторические данные).
- **Автообновление** на клиенте раз в ~60 сек (совпадает с TTL кэша на
  сервере, лишние запросы к провайдеру не идут).
- **Внешний вид:** непрерывная прокрутка CSS-анимацией
  (`@keyframes marquee` + дублированный список для бесшовной петли), без
  JS-цикла; на hover анимация замирает (`animation-play-state: paused`),
  чтобы можно было прочитать цифры; по краям — маска-градиент
  (`mask-image`), чтобы элементы плавно «растворялись», а не обрезались.
- **Каждый элемент строки:** тикер валюты, цена в USD, и 24h-изменение
  зелёным/красным со стрелкой вверх/вниз (нейтральный цвет, если данных
  нет). Разделители между элементами — тонкая точка/линия в золотом цвете
  бренда.
- **Доступность:** `prefers-reduced-motion` отключает прокрутку (строка
  становится статичным горизонтально-скроллящимся списком).
- Файлы: `src/app/api/ticker/route.ts`, `src/components/rates-ticker.tsx`,
  анимация в `globals.css`; строка встраивается в `layout.tsx` под
  `NavBar`.

**Wow-эффект строки (не просто текст, а «живые» элементы):**
- Каждая валюта — «стеклянная» пилюля (`rounded-full`, полупрозрачный фон,
  `backdrop-blur`, тонкий бордер), а не голый текст.
- У каждой валюты — своя иконка: круглый значок с градиентом в
  фирменных цветах монеты (BTC — оранжевый, ETH — синий, SOL —
  фиолетово-зелёный и т.д.) и символом валюты (₿, Ξ, ₮, ◎, $, €, ₽).
  Реализуется своим компонентом `currency-icon.tsx` инлайн (без внешних
  картинок — CSP-безопасно, не зависит от сторонних CDN и не требует
  настройки `next/image` под внешние домены).
- **Реакция на hover каждой валюты:** пилюля приподнимается и
  подсвечивается золотым свечением (`box-shadow`), бордер становится
  акцентным, иконка увеличивается и слегка поворачивается, цена из
  приглушённой становится контрастной. Вся строка при этом замирает
  (`animation-play-state: paused`), чтобы курсор не «убегал» от элемента.

### 13.3 Композиция главного экрана (широкие экраны)

Проблема узкой центральной колонки: на десктопе по бокам остаётся пустое
поле — это выглядит устаревше. Решение — **двухколоночный hero**
(как на лендингах MoonPay/Crypto.com):

- **Слева** — текстовый блок: бейдж «Live market rates», крупный
  заголовок, подзаголовок, список преимуществ (прозрачная комиссия, живые
  курсы, без регистрации) с иконками, и визуальный «якорь» бренда —
  полный логотип (`logo-full.png`) с мягким золотым свечением позади.
- **Справа** — карточка калькулятора (главное действие), приподнятая
  тенью, шириной ~420–480px.
- На `lg` и шире — сетка `lg:grid-cols-2` с выравниванием по центру по
  вертикали и общим `max-w-6xl`; на мобильных — обычная одна колонка,
  калькулятор идёт первым по важности.
- Фон широких экранов не остаётся плоским: декоративные золотые
  радиальные свечения (`blur-3xl`, низкая прозрачность,
  `pointer-events-none`) по углам, чтобы «боковины» не читались пустыми.

### 13.4 Кастомные дропдауны и адаптив

Нативный `<select>` рисуется операционной системой и не поддаётся
стилизации — на фоне остальных полей он выглядит чужеродно. Замена:
собственный компонент по паттерну **listbox** (`select-menu.tsx`):
- триггер выглядит и ведёт себя как остальные инпуты (та же рамка,
  радиус, фокус-кольцо), внутри — иконка валюты, тикер и название;
- выпадающий список — карточка с тенью, скруглениями, галочкой у
  выбранного пункта и подсветкой активного;
- клавиатурный контракт эквивалентен нативному: стрелки, Home/End,
  Enter/Space, Escape, посимвольный type-ahead; фокус остаётся на триггере,
  активный пункт объявляется через `aria-activedescendant` (WCAG 4.1.2);
- закрытие по клику вне и по Tab.

**Адаптив (проверить на 320–420px):**
- Шапка: подпись «live rates · demo» скрывается ниже `sm`, лого и отступы
  уменьшаются, название усекается. Сами ссылки ниже `sm` уезжают в
  **бургер-меню**: кнопка с `aria-expanded`/`aria-controls`, выпадающая
  панель под шапкой, закрытие по Escape (с возвратом фокуса на кнопку),
  по клику вне и по переходу на ссылку. Переключатель темы остаётся в
  шапке — он нужен в один тап. В дереве доступности одновременно видна
  только одна навигация: панель скрыта атрибутом `hidden`, десктопная —
  через `display: none`.
- Табы операций: ниже брейкпоинта `xs` (26rem) перестраиваются в сетку
  2×2; скользящий индикатор при этом прячется (одна дорожка не может
  следовать за элементами в двух рядах), а активная вкладка получает
  собственную золотую заливку.
- Таблица `/rates`: горизонтальная прокрутка в своём контейнере, название
  валюты переносится под тикер на узких экранах.

### 13.5 Клик по валюте в бегущей строке → страница валюты

Сейчас пилюли в бегущей строке реагируют на hover, но некликабельны —
пользователь ожидает перехода, а его нет. Каждая пилюля становится ссылкой
на `/rates/[code]` (например `/rates/BTC`).

**Что показывает страница валюты (`/rates/[code]`):**

1. **Шапка валюты** — крупная иконка, тикер, полное название, тип
   (fiat/crypto).
2. **Hero-цена** — текущая цена в USD крупным кеглем (пропорциональные
   цифры, не `tabular-nums` — по правилам dataviz для крупных чисел) и
   рядом изменение за 24 часа со стрелкой и цветом.
3. **График истории** — переиспользуется существующий `RateChart`
   (`CODE → USD`) с пресетами 7/30/90 дней, crosshair и табличным дублем.
   Никакого нового кода для графика не требуется.
4. **Ключевые цифры за период** — минимум, максимум и изменение за
   выбранный диапазон. Считается чистой функцией над уже загруженной
   серией (`seriesStats`), покрывается тестами.
5. **Быстрый переход к обмену** — кнопки «Buy BTC» / «Sell BTC»
   (для фиата — «Exchange USD»), которые ведут на калькулятор с
   предзаполненной парой и нужным режимом. Реализуется через query-
   параметры (`/?mode=buy&receive=BTC`), которые калькулятор читает при
   инициализации состояния.
6. **Таблица кросс-курсов** — сколько стоит 1 BTC в каждой другой
   поддерживаемой валюте (считается из уже имеющихся USD-курсов через
   `computeCrossRate`, без новых запросов к провайдерам).
7. **Крошки** — `Calculator → Live rates → BTC`, с JSON-LD, как и на
   остальных внутренних страницах.

**Доступность и поведение:**
- Пилюля превращается в `<Link>`: цель ≥ 24px (2.5.8), видимый фокус,
  доступное имя вида «Bitcoin, $63,972, down 0.03% — view details».
- Прокрутка строки замирает не только на hover, но и **на фокусе с
  клавиатуры** (`:focus-within`), иначе до ссылки невозможно доехать
  табом — она уезжает из-под курсора.
- Дубликат списка (вторая копия для бесшовной петли) уже помечен
  `aria-hidden`; его ссылки дополнительно получают `tabIndex={-1}`, чтобы
  не было двойного обхода по табу.
- На `/rates` строки таблицы тоже становятся ссылками на ту же страницу —
  один и тот же пункт назначения из двух мест.

### 13.6 Единая ширина контента (layout-система)

Проблема: ширины разъехались по мере роста проекта — шапка и баннер
`max-w-5xl`, главная `max-w-6xl`, `/rates` и страница валюты `max-w-2xl`,
история, чекаут и страницы ошибок `max-w-xl`, плюс разные горизонтальные
отступы (`px-5` против `px-4 sm:px-5` в шапке). При переходе между
страницами контент прыгает и не выравнивается по шапке — ощущение, что это
разные приложения.

Решение — **две ширины, и обе задаются одним компонентом**
(`components/layout/page-container.tsx`), а не расставляются вручную:

| Вариант | Ширина | Где |
|---|---|---|
| `frame` | `max-w-5xl` | шапка, демо-баннер, бегущая строка, двухколоночный hero главной |
| `content` | `max-w-2xl` | все внутренние страницы: `/rates`, `/rates/[code]`, `/history`, все шаги чекаута, статус заявки, 404/ошибка/загрузка |

Правила:
- Горизонтальные отступы **одинаковые везде**: `px-4 sm:px-5` — те же, что
  в шапке, иначе контент не выравнивается с логотипом по левому краю.
- `max-w-6xl` на главной убирается: hero выравнивается по шапке
  (`max-w-5xl`), чтобы левый край заголовка совпадал с логотипом.
- Узкие карточки (форма чекаута) не сужаются отдельным `max-w-xl` —
  вместо этого ширину ограничивает сама карточка внутри общей колонки,
  а контейнер остаётся общим.
- Новый экран не задаёт ширину сам — он оборачивается в `PageContainer`.

### 13.2 Анимации интерактивных элементов (состояния)

Единый набор правил, чтобы интерфейс ощущался отзывчивым и дорогим, а не
дёргался. Все переходы — на `transform`/`opacity`/`box-shadow`
(GPU-friendly), длительности короткие, кривые — плавные.

- **Тайминги:** 150–200 мс для мелких элементов (ссылки, иконки, поля),
  250–300 мс для крупных (карточки, кнопки, пилюли). Кривая —
  `cubic-bezier(0.4, 0, 0.2, 1)` (ease-out), для «упругих» элементов
  (swap-кнопка) — лёгкий overshoot.
- **Состояния для каждого интерактивного элемента:**
  - `hover` — подъём (`-translate-y-0.5`), усиление тени, акцентный бордер;
  - `active` — возврат вниз/сжатие (`scale-95`), чтобы нажатие
    чувствовалось физически;
  - `focus-visible` — золотое кольцо (`ring-2 ring-accent/25`), никогда не
    убирать outline без замены (доступность с клавиатуры);
  - `disabled` — `opacity-45` + `pointer-events-none`, без анимаций;
  - `loading` — спиннер/скелетон вместо резкой подмены контента.
- **Главная CTA-кнопка:** золотой градиент, при hover — подъём и более
  насыщенная тень акцентного цвета, при active — обратно вниз.
- **Swap-кнопка (⇅):** круглая, при hover увеличивается (`scale-110`),
  при клике — поворот на 180° (визуально «переворачивает» пару).
- **Смена курса/суммы:** значение не должно резко «прыгать» — при
  обновлении курса делается короткая подсветка/fade нового значения, чтобы
  пользователь заметил изменение (но без мерцания при каждом рендере).
- **Карточки истории:** при hover — подъём + акцентный бордер + тень,
  создаёт ощущение «кликабельного объекта».
- **Появление контента:** мягкий `fade-in`+небольшой подъём для основной
  карточки и элементов истории при первом рендере, без длинных задержек.
- **Уважение к `prefers-reduced-motion`:** все декоративные анимации
  (бегущая строка, появление, подъёмы) отключаются/сводятся к смене цвета.

---

## 14. Операции: переключатель режимов (Buy / Sell / Swap / …)

Сейчас калькулятор — один универсальный режим «отдаю / получаю». На реальных
обменниках (MoonPay, Changelly, CoinGate, Simplex) вверху виджета стоит
переключатель операции — это сразу объясняет пользователю, что он делает, и
позволяет подстроить форму под сценарий.

### 14.1 Базовые режимы (делать в первую очередь)

| Режим | Что значит | Что меняется в UI |
|---|---|---|
| **Buy** | фиат → крипта | «Отдаю» ограничено фиатом, «Получаю» — криптой; поле реквизитов = адрес кошелька; сумма вводится в фиате |
| **Sell** | крипта → фиат | зеркально: «Отдаю» — крипта, «Получаю» — фиат; поле реквизитов = банковские данные (демо) |
| **Swap** | крипта → крипта | оба селектора только крипта; поле реквизитов = адрес кошелька получателя |

Реализация: режим — состояние калькулятора, которое **фильтрует списки
валют** и подпись поля реквизитов. Доменная логика расчёта
(`computeExchangeAmount`) не меняется — режим влияет только на то, какие
пары допустимы, поэтому проверку «пара соответствует режиму» логично
добавить в `validateExchangeRequest` (чистая функция → покрывается тестами
на 100%, как остальное ядро).

**UI переключателя:** сегментированный контрол (segmented control) —
горизонтальная группа из 3 пилюль в общей «дорожке» со скользящим золотым
индикатором активного режима (плавный `transform`, в духе раздела 13.2).
Полноценно доступен с клавиатуры (`role="tablist"`, стрелки влево/вправо),
на мобильных — на всю ширину карточки.

### 14.2 Что ещё можно добавить (предложения, по приоритету)

1. **Exchange (фиат → фиат)** — 4-й режим, дешёвый в реализации: курсы фиата
   уже есть от Frankfurter, отдельная логика не нужна. Логично, если
   позиционировать проект и как конвертер валют.
2. **Rate alert** («сообщить, когда курс достигнет X») — уже намечен в
   разделе 2.2. Это не операция обмена, поэтому лучше не в тот же
   переключатель, а отдельной кнопкой/вкладкой рядом с курсом.
3. ✅ **Recurring buy** (регулярная покупка, как у MoonPay) — сильный
   портфолио-приём: показывает работу с расписанием и повторяющимися
   сущностями. В демо-режиме — просто сохранённое «расписание» без реального
   исполнения.
4. ✅ **Limit order** («обменять, когда курс станет X») — интересная доменная
   логика (сравнение целевого курса с текущим), но требует фонового
   процесса/крона, что выходит за рамки MVP без бэкенда. Кандидат на
   итерацию 2.
5. **Sandbox/«Try with demo balance»** — виртуальный баланс, чтобы
   пользователь мог «покупать» без ввода реквизитов. Честно вписывается в
   демо-природу проекта и убирает трение на первом экране.

**Рекомендация:** сделать Buy / Sell / Swap (14.1) + Exchange как самый
дешёвый четвёртый режим; остальное — после, чтобы не размывать один
понятный сценарий.

## 17. Алерты по курсу (раздел 2.2)

«Сообщить, когда курс достигнет X». Демо-версия: **никаких писем и push
не отправляется** — срабатывание видно в самом приложении. Это честно и
не требует бэкенда, почтового сервиса и согласия на рассылку.

### 17.1 Модель и доменная логика

```ts
type RateAlert = {
  id: string;
  createdAt: string;
  giveCurrency: string;    // пара, за которой следим: A → B
  receiveCurrency: string;
  direction: "above" | "below"; // сработать, когда курс выше/ниже цели
  targetRate: number;
  rateAtCreation: number;  // курс на момент создания — для контекста в UI
  triggeredAt: string | null; // null = ещё ждёт
};
```

Чистые функции (`lib/alerts.ts`, покрываются тестами на 100%):
- `isAlertTriggered(alert, currentRate)` — сработал ли (с учётом
  направления и того, что уже сработавший повторно не срабатывает);
- `suggestDirection(targetRate, currentRate)` — если пользователь ввёл
  цель выше текущего курса, направление явно `above`, и наоборот; это
  избавляет от лишнего переключателя в UI и от бессмысленных алертов
  («сработать, когда курс выше 100», когда он уже 200);
- `validateAlert(input)` — цель положительная, пара поддерживается и
  валюты различаются, цель не совпадает с текущим курсом.

### 17.2 Проверка срабатывания

Без бэкенда «следить» можно только пока открыта вкладка — это надо
честно написать в UI, а не делать вид, что алерт работает офлайн.

- Проверка идёт там же, где уже обновляются курсы: клиент раз в 60 сек
  дёргает `/api/ticker`, и на каждом ответе прогоняются все ждущие алерты
  через `isAlertTriggered`.
- Сработавший алерт получает `triggeredAt` и остаётся в списке как
  «сработал» — не удаляется, чтобы пользователь увидел результат.
- Уведомление в интерфейсе: небольшая плашка (toast) + счётчик у пункта
  меню «Alerts». Никаких `Notification` API и разрешений браузера — это
  создало бы впечатление реальной доставки.

### 17.3 Экраны

- Форма создания — на странице валюты (`/rates/[code]`), рядом с ценой:
  поле цели, подсказка текущего курса, кнопка «Notify me».
- `/alerts` — список: ждущие сверху, сработавшие ниже, у каждого пара,
  цель, текущий курс и расстояние до цели в процентах; удаление алерта.
- Хранение — `localStorage`, тем же паттерном, что заявки
  (`useSyncExternalStore`, чтобы список обновлялся без опроса).

---

## 21. Проверка на устройствах: найденные баги и предложения

Первый реальный прогон в браузере (Playwright, 1440 / 768 / 390, обе темы).
Общее состояние: вёрстка держится, шапка и табы 2×2 работают как задумано,
график после переноса занимает всю ширину, тёмная тема ровная.

### 21.1 Баги (по убыванию важности)

1. **Подписи «STEP 1/2/3» на фото нечитаемы.** На главной, в карточках
   «How it works», номер шага набран `text-muted` поверх фотографии.
   Градиентная подложка идёт снизу, а подпись стоит на самой картинке —
   контраст проваливается ниже 4.5:1 и зависит от того, что оказалось под
   текстом. Нарушение WCAG 1.4.3. **Чинить:** дать подписи собственную
   подложку-чип (как у иконки рядом) либо перенести её в тело карточки.
2. **На `/rates` колонка «24h» уезжает за экран.** У таблицы `min-w-md`
   (448px), контейнер прокручивается горизонтально, но на 390px видно
   только валюту и цену. Самая интересная колонка спрятана, и подсказки,
   что там есть прокрутка, нет. **Чинить:** на узких экранах не третья
   колонка, а изменение под ценой в той же ячейке — тогда ничего не
   скрыто и прокрутка не нужна.
3. *(мелочь)* Кнопка паузы бегущей строки прижата к левому краю окна и
   визуально сливается с первой пилюлей.

### 21.2 Существенные улучшения (предложения)

- ✅ **OG-картинка, `sitemap.xml`, `robots.txt`.** Для портфолио важнее, чем
  кажется: ссылку на проект отправляют в мессенджер, и сейчас превью
  пустое. OG можно генерировать через `next/og` из тех же токенов бренда.
- ✅ **Оси у графика.** Подписаны минимум и максимум диапазона.
- ~~Осей у графика нет.~~ Значения читаются только через тултип и
  таблицу. Две подписи — минимум и максимум диапазона — дали бы масштаб
  без наведения.
- ✅ **Фиксация курса** — поднята до 5 минут. ~~2 минуты~~ на шаге подтверждения истекает, пока
  человек читает страницу. Продлить до 5 минут либо не запускать отсчёт,
  пока вкладка неактивна.
- ✅ **Мониторинг ошибок.** ~~Нет мониторинга.~~ `error.tsx` показывает пользователю
  сообщение, но наружу ничего не уходит — на бою узнать о поломке нечем.
- ✅ **`/rates/[code]` отдаёт настоящий 404.** ~~Для неизвестного кода~~ отдаёт 404-страницу, но со
  статусом 200 (ограничение клиентского компонента). Для SEO стоит
  перевести страницу на серверную.


_Раздел 21.2 — закрыт полностью. Две находки по ходу, обе про то, что статус
ответа врал:_

1. _`notFound()` из клиентского компонента рисует 404-экран, но отвечает 200._
   _Страница валюты переведена на серверную, и добавлен `dynamicParams: false`_
   _— без него Next рендерил неизвестный код и **клал результат в кэш**
   пререндера, так что `/rates/NOPE` продолжал отвечать 200._
2. _С `dynamicParams: false` страницы существуют только под кодами в верхнем_
   _регистре. Локально на Windows `/rates/btc` открывался (файловая система_
   _нечувствительна к регистру), а на Linux в Vercel это был бы 404 — поэтому_
   _middleware приводит код к верхнему регистру редиректом 308, заодно давая_
   _один канонический URL на валюту._

---

## 20. Крупная форма страниц: ширины, воздух, фон, прокрутка

Отзыв после деплоя: мелкие элементы выглядят хорошо, а **крупная форма
примитивна**. Конкретно:
1. Ширина главной не совпадает с шириной остальных страниц.
2. Внутренние страницы слишком узкие на широких экранах — всё сжато в
   центр, по бокам пустота.
3. На главной есть и пустоты, и наоборот ужатые места.
4. Не хватает фона: людей/видео, чего-то живого и современного.
5. Нет глобальной анимации при прокрутке.

### 20.1 Ширины: раздел 13.6 был решён слишком узко

Прошлая унификация свела всё к `max-w-2xl` (42rem) — это ширина колонки
текста, а не приложения. На 1440–2560px это выглядит как узкая лента по
центру. Пересмотр:

| Слой | Было | Стало |
|---|---|---|
| Внешний контейнер (шапка, все страницы) | 5xl / 2xl вразнобой | **`max-w-6xl` (72rem) везде** |
| Колонка текста внутри | — | `max-w-prose` там, где это абзацы |

Принцип: **контейнер один и тот же на всех страницах**, а «узость»
задаётся не контейнером, а раскладкой внутри. Широкий экран должен
получать больше контента, а не больше пустоты:
- `/rates` — таблица на всю ширину;
- `/rates/[code]` — на `lg` две колонки: слева цена + график, справа
  кросс-курсы и форма алерта (сейчас всё в одну узкую колонку);
- `/alerts`, `/history` — список на `lg` в две колонки карточек;
- чекаут — карточка шага слева, сводка заказа справа (сейчас сводка
  спрятана внутрь шага, а справа пусто).

### 20.2 Фон и «крупный» дизайн главной

- Полноэкранная hero-секция с фоновым слоем: **видео или фото**. Важно:
  ассеты с людьми нельзя брать откуда попало — нужен источник со
  свободной лицензией (Unsplash / Pexels) и указание автора, либо
  сгенерированный абстрактный фон. Пока ассета нет, слот занимает
  CSS-композиция: анимированный градиентный «меш», сетка и мягкое золотое
  свечение — без внешних файлов и лишнего веса.
- Затемняющий слой поверх фона, чтобы текст всегда держал контраст 4.5:1
  независимо от того, какая картинка окажется под ним.
- Секции ниже hero: «как это работает» (3 шага), «почему это демо»,
  живые курсы — чтобы страница перестала быть одним экраном с
  калькулятором и получила ритм.

### 20.3 Анимация при прокрутке

- Элементы появляются при входе в вьюпорт (`IntersectionObserver`,
  «поднялся + проявился»), с небольшой задержкой по индексу внутри
  группы — но **однократно**, без повторов при обратной прокрутке.
- Параллакс фона hero — сдвиг фонового слоя медленнее контента.
- Всё — только `transform`/`opacity`.
- `prefers-reduced-motion` отключает и появление, и параллакс:
  элементы просто сразу видимы (важно не оставить их в состоянии
  `opacity: 0` навсегда — это классическая ошибка таких эффектов).

---

## 19. Блик на кнопках (hover / active) — wow-эффект

Сейчас кнопки на hover только приподнимаются и меняют тень. Хочется
дорогого ощущения: по кнопке **пробегает блик** — как отсвет по
полированному металлу. Это ложится на золотой бренд лучше любого
изменения цвета.

### 19.1 Два варианта блика (использовать оба, для разных ролей)

1. **Блик по поверхности** — для главных (золотых) кнопок. Поверх
   градиента лежит узкая диагональная светлая полоса
   (`linear-gradient(105deg, transparent 40%, rgba(255,255,255,.55) 50%, transparent 60%)`),
   которая на hover проезжает слева направо за ~700 мс. Реализация —
   псевдоэлемент `::after` с `transform: translateX(-100%) → 100%` и
   `overflow: hidden` на кнопке.
2. **Блик по рамке** — для вторичных кнопок с обводкой, где заливки нет и
   блику негде пробежать. Рамка рисуется коническим градиентом через
   `border-image` / маску, и на hover угол градиента прокручивается на
   360°, отчего по контуру бежит светлая точка.

### 19.2 Правила, чтобы это не выглядело дёшево

- **Один прогон, не бесконечный цикл.** Блик проезжает один раз при
  наведении и один раз при `:active`. Зацикленное мерцание превращает
  кнопку в рекламный баннер и отвлекает от контента.
- **Только для главных действий.** Золотой CTA («Continue», «Buy BTC»,
  «Create request») — да. Иконки, ссылки в шапке, пункты списков — нет,
  иначе эффект обесценивается.
- **Блик не должен красть контраст.** Полоса светлая и полупрозрачная, но
  текст на кнопке — чёрный по золоту (8:1); при прогоне контраст в моменте
  не должен падать ниже 4.5:1, поэтому непрозрачность блика ограничена
  ~0.55 и он не задерживается под текстом.
- **`prefers-reduced-motion`** — блик полностью отключается (как и
  остальные анимации проекта), остаётся статичное изменение тени.
- Анимируются только `transform` и `opacity` — они идут на композиторе и
  не вызывают перерисовку макета.

### 19.3 Где применить

- `.gold-surface` кнопки: калькулятор («Continue to checkout»), все шаги
  чекаута, «Buy/Sell/Exchange» на странице валюты, «Try again» на
  странице ошибки.
- Вторичные кнопки с рамкой: «Sell BTC», «View history», «Notify me».
- Реализовать как утилитарные классы в `globals.css` (`.sheen`,
  `.sheen-border`), чтобы не дублировать разметку в каждом компоненте.

---

## 18. Перетаскивание бегущей строки (drag-to-scroll)

Идея: схватить строку за пустое место и потянуть влево/вправо, чтобы
самому долистать до нужной валюты, не дожидаясь, пока она приедет.

### 18.1 Главная сложность — уживание с CSS-анимацией

Сейчас лента едет через `@keyframes` + `animation`, а позиция при
перетаскивании задаётся из JS. Два источника правды по одной и той же
координате конфликтуют, поэтому:

- при `pointerdown` анимация ставится на паузу (`animation-play-state`),
  запоминается текущее смещение, дальше позиция управляется вручную через
  `transform: translateX(...)`;
- при отпускании лента **не прыгает обратно** — анимация возобновляется с
  того места, куда её дотащили. Для этого текущее смещение переводится в
  отрицательную `animation-delay` (или лента переносится на бесшовный
  эквивалент позиции по модулю ширины одной копии списка).

Бесшовность петли уже обеспечена дублем списка: при уходе за границу
одной копии смещение нормализуется по модулю её ширины, поэтому тянуть
можно бесконечно в обе стороны.

### 18.2 Перетаскивание не должно ломать клики

Пилюли — ссылки, а значит нужно отличать «клик» от «потащил»:
- порог в ~5px: пока указатель не сдвинулся дальше, это клик, ссылка
  срабатывает как обычно;
- после превышения порога — `setPointerCapture`, а на `click` ставится
  `preventDefault`, иначе отпускание пальца над пилюлей откроет страницу
  валюты, которую пользователь просто протаскивал мимо;
- курсор меняется на `grab` / `grabbing`, чтобы намерение читалось.

### 18.3 Доступность и поведение

- **Это дополнение, а не единственный способ.** Перетаскивание мышью
  недоступно с клавиатуры, поэтому уже существующие пауза и
  `:focus-visible` остаются основным способом добраться до пилюли —
  drag ничего из этого не заменяет.
- Горизонтальный свайп на тач-экранах не должен мешать вертикальной
  прокрутке страницы: `touch-action: pan-y`, чтобы браузер сам отдавал
  вертикальные жесты странице, а горизонтальные — ленте.
- При `prefers-reduced-motion` автопрокрутки нет, но **перетаскивание
  остаётся** — оно инициируется пользователем, а значит не нарушает
  предпочтение; наоборот, это единственный способ долистать ленту в этом
  режиме, если она шире экрана.
- Инерция (продолжение движения по «броску») — опционально, второй
  итерацией: считается скорость последних событий, дальше затухание. Без
  неё жест работает, просто ощущается суше.

---

## 15. Многошаговый флоу оплаты (после создания заявки)

Сейчас заявка создаётся и сразу попадает на страницу статуса. Нужен
пошаговый флоу, как у настоящих обменников.

### 15.1 Шаги

1. **Калькулятор** (`/`) — как сейчас, создаёт заявку.
2. **Метод оплаты** (`/exchange/[id]/method`) — выбор способа: карта,
   банковский перевод, крипто-кошелёк (депозит), а также «демо-баланс»
   (мгновенно, без реквизитов).
3. **Реквизиты** (`/exchange/[id]/details`) — форма зависит от метода:
   - карта → номер/срок/CVC (демо-значения);
   - банковский перевод → IBAN/имя получателя;
   - крипто → адрес кошелька, куда пользователь получит средства;
   - демо-баланс → шаг пропускается.
4. **Подтверждение** (`/exchange/[id]/confirm`) — сводка (сумма, курс,
   комиссия, метод, замаскированные реквизиты), таймер фиксации курса и
   кнопка «Confirm & pay». Для карты после подтверждения — имитация
   **3-D Secure/OTP** (любой код проходит, о чём явно написано). Для
   крипто-метода вместо OTP — экран **депозитного адреса с QR** и таймером.
5. **Статус** (`/exchange/[id]`) — анимированный трекер стадий:
   `awaiting payment → payment received → exchanging → sending → completed`
   с искусственными задержками и фейковым TX-хешем.

Все три варианта финала (подтверждение+трекер, OTP, депозитный адрес+QR)
не взаимоисключающие — они выбираются в зависимости от метода оплаты.

### 15.2 Управление шагами (защита от перескока)

Источник истины — **поле `step` внутри самой заявки** (сохраняется в
localStorage сейчас и в БД позже), а не URL:

```ts
type ExchangeStep = "method" | "details" | "confirm" | "otp" | "deposit" | "status";
```

- У каждого шага есть порядковый индекс. При заходе на URL шага
  сравниваем его индекс с индексом сохранённого `step`:
  - индекс URL **больше** сохранённого → редирект на актуальный шаг
    (перескочить вперёд нельзя);
  - индекс URL **меньше или равен** → показываем (назад ходить можно, чтобы
    поправить метод/реквизиты);
  - заявки с таким id нет → страница «not found».
- Логика сравнения — чистая функция (`resolveStepAccess`) в доменном слое,
  покрывается тестами вместе с остальным ядром.
- Прогресс-индикатор (1-2-3-4) виден на всех шагах, пройденные шаги
  кликабельны, будущие — нет.

### 15.3 Безопасность демо (никаких реальных переводов)

Ключевое требование: должно **выглядеть** как реальная оплата, но
пользователь физически не может отправить деньги.

- **Никаких реальных платёжных интеграций** — форма ничего не отправляет на
  внешние сервисы, данные остаются в localStorage браузера.
- **Карта:** поле предзаполнено общеизвестным тестовым номером
  (`4242 4242 4242 4242`), ввод реальных данных не требуется и явно не
  нужен; сохраняем только последние 4 цифры, никогда — CVC.
- **Крипто-депозит:** адрес намеренно **невалидный** (содержит
  `DEMO`-префикс и не проходит проверку контрольной суммы), поэтому
  кошелёк пользователя откажется по нему отправлять. QR кодирует
  текстовую строку-предупреждение, а не платёжный URI.
- **Банковский перевод:** реквизиты помечены как демонстрационные, IBAN не
  принадлежит реальному счёту.
- На каждом платёжном шаге — заметная плашка «Demo — no real payment is
  processed», в дополнение к глобальному баннеру.

## 16. Доступность (WCAG 2.2, уровни A и AA)

Цель — соответствие **WCAG 2.2 уровня AA** (включает все критерии уровня A).
Для портфолио это сильный сигнал: показывает, что автор думает не только о
красоте, но и о том, что интерфейсом реально можно пользоваться.

### 16.1 Что нужно исправить/обеспечить

**Уровень A:**
- **1.1.1 Non-text Content** — у всех иконок либо `aria-hidden` (если рядом
  есть текст), либо текстовая альтернатива. Декоративный QR уже помечен как
  «not scannable» через `aria-label`.
- **1.3.1 Info and Relationships** — семантическая разметка: `<nav>`,
  `<main>`, списки для перечислений, `<dl>` для пар «поле → значение»,
  корректная иерархия заголовков (на каждой странице ровно один `<h1>`).
- **1.4.1 Use of Color** — цвет не единственный носитель смысла: рост/падение
  в бегущей строке дублируются стрелками, статусы — иконками и текстом.
- **2.1.1 Keyboard** — весь флоу проходится с клавиатуры; segmented control
  режимов работает стрелками (`role="tablist"`).
- **2.2.1 Timing Adjustable** — таймеры (фиксация курса, окно депозита) не
  должны блокировать пользователя: по истечении даётся явное действие
  «обновить котировку», а сам таймер можно продлить.
- **2.2.2 Pause, Stop, Hide** — **критично для бегущей строки**: анимация
  идёт дольше 5 секунд и запускается автоматически, поэтому нужна видимая
  кнопка паузы (одного `:hover` недостаточно — с клавиатуры и на тач-экране
  он недоступен).
- **2.4.1 Bypass Blocks** — ссылка «Skip to content» в начале страницы,
  видимая при фокусе.
- **2.4.2 Page Titled** — у каждого роута свой осмысленный `<title>`.
- **3.3.1 Error Identification** — ошибки форм объявляются
  ассистивным технологиям (`role="alert"`), поле помечается `aria-invalid`
  и связывается с текстом ошибки через `aria-describedby`.
- **4.1.2 Name, Role, Value** — у всех контролов доступное имя; состояние
  передаётся через `aria-selected`/`aria-checked`/`aria-current`.

**Уровень AA:**
- **1.4.3 Contrast (Minimum)** — контраст текста ≥ 4.5:1 (крупного ≥ 3:1).
  Проверить приглушённый текст (`--muted`) и золотой акцент в обеих темах;
  при необходимости затемнить золото для светлой темы.
- **1.4.11 Non-text Contrast** — границы полей, иконки и индикатор фокуса
  ≥ 3:1 к фону.
- **1.4.10 Reflow / 1.4.4 Resize Text** — вёрстка не ломается при 200%
  масштабе и на узких экранах (уже отчасти обеспечено адаптивной сеткой).
- **2.4.7 Focus Visible** — видимый индикатор фокуса у **каждого**
  интерактивного элемента (сейчас есть не везде — добить ссылки в шапке,
  элементы бегущей строки, ссылки в истории).
- **2.4.11 Focus Not Obscured** — залипающая шапка не должна перекрывать
  сфокусированный элемент → `scroll-margin-top` у якорных целей.
- **2.5.8 Target Size (Minimum)** — интерактивные цели не меньше 24×24 CSS-
  пикселей.
- **3.2.3/3.2.4 Consistent Navigation & Identification** — шапка, крошки и
  прогресс шагов выглядят и ведут себя одинаково на всех экранах.

### 16.2 Хлебные крошки со schema.org

Крошки нужны и как навигация (пользователь видит, где он в многошаговом
флоу), и как разметка для поисковиков.

- Компонент `breadcrumbs.tsx`: `<nav aria-label="Breadcrumb">` со списком
  `<ol>`, текущий элемент помечен `aria-current="page"` и не является
  ссылкой.
- **Структурированные данные schema.org** — `BreadcrumbList` в формате
  **JSON-LD** (рекомендованный Google формат) внутри `<script
  type="application/ld+json">`: массив `itemListElement` из `ListItem` с
  полями `position`, `name`, `item` (абсолютный URL).
- Крошки показываются на внутренних страницах: `Home → History`,
  `Home → History → Request → <шаг>`. На главной не показываются (там
  крошка была бы единственной и бессмысленной).
- Так как страницы клиентские, JSON-LD рендерится тем же компонентом; для
  абсолютных URL берётся `NEXT_PUBLIC_SITE_URL` с безопасным фолбэком на
  текущий origin.

---

_Статус: MVP (раздел 9, шаги 1–7, 10) реализован — работающий калькулятор,
живые курсы, демо-заявки в localStorage, история, тема, демо-баннер, README,
100% покрытие доменной логики тестами, чистые `tsc`/`build`/`eslint`._

_Раздел 12 (бренд) — ✅ сделано: проект переименован в **Crypto Exchanger**,
логотипы (`logo-mark`, `logo-full` в PNG+SVG) получены из оригинального
рендера с прозрачным фоном, фавикон собран из знака без пустых полей._

_Раздел 13 (дизайн) — ✅ сделано: золотисто-чёрные токены, двухколоночный
hero на широких экранах (13.3), sticky-шапка с blur, редизайн калькулятора,
истории и страницы заявки, единая анимация состояний (13.2)._

_Раздел 13.1 (бегущая строка) — ✅ сделано: `GET /api/ticker` на реальных
данных (CoinGecko + Frankfurter, 24h change), компонент `rates-ticker.tsx`
с иконками валют, hover-реакцией и паузой прокрутки._

_Раздел 15 (многошаговый флоу оплаты) — ✅ сделано: шаги method → details →
confirm → otp/deposit → status на отдельных URL, порядок шагов защищён полем
`step` внутри заявки через `resolveStepAccess` (17 тестов), трекер стадий с
фейковым TX-хешем, все демо-предохранители из 15.3 на месте. Заодно опросы по
таймеру заменены на `useSyncExternalStore` над localStorage._

_Раздел 14.1 (режимы операций) — ✅ сделано: Buy / Sell / Swap / Exchange
как segmented control со скользящим золотым индикатором, списки валют
фильтруются под режим, swap-кнопка инвертирует режим (Buy ↔ Sell), режим
сохраняется в заявке и показывается на шаге подтверждения. Логика в
`operations.ts`, покрыта тестами. Раздел 14.2 (recurring/limit/alerts) —
не делали._

_Раздел 2.2 / шаг 9 (расширенный функционал) — ✅ частично: график истории
курса выбранной пары (7/30/90 дней) на живых данных (`GET /api/history`,
CoinGecko `market_chart` + Frankfurter timeseries, кросс-курс через USD,
кэш 15 мин) и страница `/rates` со всеми поддерживаемыми валютами. График
сделан по методике dataviz: одна серия → без легенды, линия 2px, заливка-
градиент, hairline-сетка, crosshair с тултипом, табличный дубль для
доступности, пресеты диапазона одной строкой над графиком, при перезагрузке
данных держится предыдущий рендер. Цвета линии (`--chart-line`: `#8a6413`
светлая / `#b08828` тёмная) прогнаны через валидатор палитры. Не сделано из
2.2: алерты по курсу, мультиязычность._

_Итерация 2 (БД) — 🚧 код готов, ждёт подключения: Neon Postgres + Drizzle,
схема из трёх таблиц (`exchange_requests`, `rate_alerts`, `recurring_plans`),
группировка по анонимной сессии в httpOnly-cookie вместо auth, API-роуты с
валидацией zod и upsert по клиентскому id. **БД опциональна:** без
`DATABASE_URL` роуты отвечают 501, клиент это запоминает и продолжает жить на
localStorage. localStorage остаётся источником правды для UI, БД — зеркалом,
переживающим очистку кэша; при загрузке строки с сервера подмешиваются по id
(`hydrateFromServer`). **Остался ручной шаг владельца:** создать проект в
Neon, положить `DATABASE_URL` в `.env.local` и в переменные Vercel, выполнить
`npm run db:push`. Auth не делали — заявки анонимные, и cookie честно назван
не авторизацией._

_Фото (`public/photos`) — ✅ встроены в hero и карточки главной, обесцвечены
и подтонированы в золото под бренд, каждое под скримом. iStock-превью вынесено
из `public/`: это comp-preview, а не лицензия на публикацию._

_Раздел 14.2 — ✅ Recurring buy: страница `/recurring`, расписания
daily/weekly/monthly с чистой логикой дат (`recurring.ts`, покрыта тестами),
пауза/возобновление, дата следующего запуска и прогноз трат за 90 дней.
Ничего не исполняется, и это сказано в интерфейсе прямо. **Limit order
осознанно не делали:** без бэкенда он выродился бы в тот же браузерный
наблюдатель, что и алерты, только с лишними шагами — честнее оставить его
на итерацию 2 вместе с БД._

_Раздел 13.6 (единая ширина) — ✅ сделано: две ширины через
`components/layout/page-container.tsx` (`frame` = `max-w-5xl` для шапки,
баннера и hero главной; `content` = `max-w-2xl` для всех внутренних
страниц), одинаковые отступы `px-4 sm:px-5` везде. Раньше ширины гуляли от
`max-w-xl` до `max-w-6xl`, и контент не выравнивался с шапкой._

_Раздел 2.1 — ✅ закрыт полностью: доделан обратный ввод суммы (ввод с любой
стороны пересчитывает другую). Покрытие тестами расширено на все чистые
модули (`checkout-flow`, `operations`, `rate-history` добавлены в
`vitest.config.ts` — раньше порог 100% проверялся только на двух файлах из
пяти): 111 операторов, 90 ветвлений, 21 функция — 100%._

_Раздел 13.4 — ✅ сделано: нативные `<select>` заменены на свой listbox
(`ui/select-menu.tsx`) с полным клавиатурным контрактом и type-ahead;
адаптированы шапка (скрытая подпись, компактные отступы), табы операций
(2×2 ниже `xs`) и таблица `/rates` (горизонтальная прокрутка). Живьём на
устройствах не проверено: MCP Playwright в этой сессии отключён, проверка
шла по разметке и вручную в браузере._

_Раздел 16 (WCAG 2.2 A/AA) — ✅ сделано: кнопка паузы бегущей строки (2.2.2),
продление таймеров (2.2.1), skip-link (2.4.1), фокус-кольца везде (2.4.7),
заголовки роутов (2.4.2), `role="alert"`/`aria-invalid`/`aria-describedby` в
формах (3.3.1), `aria-current` в шапке (4.1.2). Контраст (1.4.3/1.4.11)
пересчитан: светлое золото затемнено до `#8a6413`, добавлены токены
`--control-border`, `--success`, `--danger` и отдельные стопы градиента для
светлой темы. Раздел 16.2 — ✅ крошки с JSON-LD `BreadcrumbList` на истории,
статусе и всех шагах чекаута._

_Раздел 10 (чеклист): ✅ добавлены `not-found.tsx`, `error.tsx`,
`global-error.tsx`, `loading.tsx`. Заодно исправлен баг со старыми заявками
без поля `step`, которые давали ссылки `/exchange/<id>/undefined` —
теперь такие записи нормализуются при чтении._

_Попутно: фиат-список изменён с USD/EUR/RUB на USD/EUR/GBP/JPY — Frankfurter
больше не публикует RUB, и пары с рублём отдавали "rate unavailable"._

**Что осталось (на момент последней сессии):**

1. **Подключить Neon** — код готов и задеплоен, но БД ещё не создана. Три шага
   владельца: создать проект на neon.tech → положить pooled-строку в
   `DATABASE_URL` (`.env.local` + переменные окружения Vercel) →
   `npm run db:push`. Пока переменной нет, приложение работает на localStorage,
   как и раньше, — это штатный режим, а не поломка.
2. **Проверить на устройствах** — вёрстка адаптирована, но живьём на телефоне
   и планшете не смотрели.
3. ✅ **Limit order** — сделан. Ордер ждёт цену и исполняется **по рыночному
   курсу в момент пересечения**, а не по цели: рынок может проскочить её
   гэпом, и именно это отличает лимитный ордер от алерта. Логика в
   `limit-orders.ts`, покрыта тестами; проверка идёт тем же опросом, что и
   алерты, — один таймер на оба, а не два запроса за теми же ценами.
4. ✅ **Auth** — сделана своя, на Neon: `users` + `auth_sessions`, scrypt с солью
   и `timingSafeEqual`, в базе только SHA-256 токена сессии. Вход требуется
   **только** для `/history`, `/alerts`, `/recurring`; калькулятор, курсы и весь
   чекаут открыты — иначе это противоречило бы тезису «No account needed» на
   самой главной. Записи, созданные до входа, усыновляются аккаунтом. Защита в
   два слоя: middleware отдаёт настоящий 307 по наличию cookie, а layout
   проверяет сессию в базе — гарда в layout одного мало, потому что после
   начала стриминга `redirect()` уезжает в тело ответа со статусом 200.


## 22. Настоящий бэкенд: расписание, почта, OAuth, passkeys

5. ✅ **Исполнение на сервере.** Раньше алерты, лимитные ордера и recurring-планы
   срабатывали только пока открыта вкладка — это демо расписания, а не
   расписание. Теперь `/api/cron/run` по Vercel Cron раз в час забирает курсы
   один раз, проверяет алерты, исполняет пересечённые ордера по рыночному курсу
   и создаёт заявки по подошедшим планам. Идемпотентность через `lastRunAt`:
   повторный запуск в том же окне ничего не дублирует. Роут закрыт
   `CRON_SECRET`, иначе кто угодно дёргал бы исполнение чужих планов.
6. ✅ **Реальная доставка алертов.** Resend по REST, без SDK. Письмо уходит
   только по алертам, привязанным к аккаунту, — анонимной записи некуда писать.
   Без `RESEND_API_KEY` отправка молча выключена, алерт всё равно
   отмечается сработавшим.
7. ✅ **OAuth GitHub + Google.** Каждый провайдер опционален, кнопка появляется
   только при настроенных ключах. Защита от login-CSRF через `state` в
   httpOnly-cookie.
8. ✅ **Passkeys.** Регистрация из панели на `/history`, вход с `/login` без
   ввода логина. Challenge хранится в БД (инстансы не делят память) и
   удаляется при чтении. Счётчик подписи обязан расти — иначе это реплей или
   клон ключа.

**Что нужно от владельца в переменных Vercel:** `CRON_SECRET`,
`NEXT_PUBLIC_SITE_URL` (без него неверный rpID у passkeys и относительные
OG-ссылки), при желании `RESEND_API_KEY` + `EMAIL_FROM`,
`GITHUB_CLIENT_ID/SECRET`, `GOOGLE_CLIENT_ID/SECRET`. Callback-адреса:
`https://ДОМЕН/api/auth/oauth/{github|google}/callback`.
