export function DemoBanner() {
  return (
    <div className="w-full bg-accent/10 border-b border-border text-center text-sm py-2 px-4">
      <span className="font-medium">Demo project</span> — no real funds are transferred. Rates
      are live, exchange requests are simulated.
    </div>
  );
}
